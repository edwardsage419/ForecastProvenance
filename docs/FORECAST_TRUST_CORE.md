# Forecast Trust Core

Version: 0.4
Status: FROZEN FOR SYNTHETIC IMPLEMENTATION

## Purpose

Forecast Trust Core defines the minimum model neutral contracts required to determine whether a forecast record is internally coherent, temporally admissible, externally anchored, reproducibly bound to its dependencies, protected against selective omission and output selection, and valid under the accepted protocol state.

It does not establish predictive skill.

## Normative documents

The frozen design is distributed across:

1. `TRUST_CORE_CONTRACTS.md` for object families and derived lifecycle rules.
2. `CANONICALIZATION_AND_IDENTIFIERS.md` for deterministic bytes, hashes, identifiers, and dependency references.
3. `TRUSTED_MANIFEST.md` for trust roots and historical manifest selection.
4. `POINT_IN_TIME_RULES.md` for forecast evidence eligibility.
5. `SUPPORTING_NORMATIVE_CONTRACTS.md` for source, transformation, cycle, anchor, validation, and review objects.
6. `POLICY_CONTRACTS.md` for consequential versioned policy objects.
7. `ISSUANCE_AND_RETRY_POLICY.md` for cycle completeness, retry, correction, and withdrawal rules.
8. `EXECUTION_SELECTION_CONTROL.md` for hidden output selection controls.
9. `PROSPECTIVE_TIME_SEMANTICS.md` for plan and forecast external time inequalities.
10. `MODEL_EVIDENCE_OBSERVABILITY.md` for bounded provenance claims about model information state.
11. `RESOLUTION_EVIDENCE_SEMANTICS.md` for outcome evidence admissibility.
12. `GENESIS_BOOTSTRAP_GOVERNANCE.md` for the out of graph Genesis authority boundary.
13. `ADVERSARIAL_REVIEW_MATRIX.md` for 96 synthetic attack cases.
14. `FTC_001_FREEZE_REVIEW.md` for the final design acceptance record.

## Core trust principles

Object identity and content identity are separate. Consequential dependencies bind both semantic object ID and full SHA256.

Candidate objects cannot select their own trusted manifest or governance root.

Forecasts and evidentiary events are immutable. Lifecycle and prospective eligibility are derived by validators.

Confirmatory cycle slots are generated by a historical manifest bound IssuanceSchedulePolicy rather than operator discretion.

Stochastic confirmatory methods require postcommit public randomness or independently auditable complete request accounting. Uncontrolled nondeterminism cannot receive confirmatory prospective status.

Forecast evidence point in time rules fail closed on unknown consequential availability.

Outcome resolution evidence follows a separate precommitted admissibility contract.

Historical validation and current verifiability are distinct claims.

## Genesis boundary

FTC_001 freezes Trust Core interfaces and scientific invariants. It does not instantiate Genesis.

Concrete OpenTimestamps Bitcoin proof semantics, bootstrap governance keys, initial targets, schedule parameters, resolution sources, forecast methods, scoring rules, and the Genesis manifest remain future Genesis blockers.

## Current authorization

FTC_002 may implement these frozen contracts using synthetic fixtures only and execute the adversarial matrix.

Forecast Ledger creation, genuine prospective forecast issuance, and production forecast operation remain prohibited.