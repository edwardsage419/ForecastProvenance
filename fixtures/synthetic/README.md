# Synthetic fixtures

Every fixture in this directory has origin class `SYNTHETIC` and is permanently ineligible for native prospective history.

`adversarial_registry.json` records synthetic execution coverage for ADV001 through ADV096. All 96 attack paths are exercised at the Trust Core interface level. This does not manufacture positive external evidence: genuine human authenticity, real source availability, bootstrap authority identity, and external time attestations remain external inputs.
